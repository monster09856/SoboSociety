services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: sobo
      POSTGRES_PASSWORD: sobo
      POSTGRES_DB: sobo
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sobo"]
      interval: 2s
      timeout: 3s
      retries: 15
